using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class instruction : MonoBehaviour
{
    /*
     * 
     * Copier coller:
     * Background
     * OptionsMenu
     * 
     * dans le canvas contenant le game
     * 
     * Dans le bouton back ajouter le pannel du jeu dans le premier OnClick, function: GameObject.SetActive(bool)  et cocher la petite case 
     * 
     * si les options de son de marchent pas demander a DRU!
     * 
     * c'est tout normalement !
     */
}
